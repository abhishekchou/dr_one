cmake_minimum_required(VERSION 2.8.3)
project(dr_one_setup)

find_package(catkin REQUIRED COMPONENTS
  roscpp
  rospy
  std_msgs
  tf
  actionlib
  actionlib_msgs
  geometry_msgs
  nav_msgs
)

generate_messages(
  DEPENDENCIES
  std_msgs  
  geometry_msgs
 )

catkin_package()
